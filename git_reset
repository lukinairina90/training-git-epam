Отменить коммит (файлы остаются проиндексированными)
    git reset --soft HEAD~1

Отменить коммит и индексацию (отменяем git add и git commit)
    git reset --mixed HEAD~1

Удаляем нахрен все изменения (не рекомендасьон)
    git reset --hard HEAD~1